workflow: invalid-missing-dep

description: Test fixture for invalid dependency (step depends on non-existent step)

mode: sequential

steps:
  - id: implementer
    role: implementer
    agent_role: agents/implementer.md
    execution_spec: execution/implementer.yaml
    task_id: "000_template"
    depends_on:
      - NONEXISTENT
